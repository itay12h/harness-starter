# Tasks (example)

Copy this to `ralph/tasks.md` and replace with your real, checkable tasks.
Keep each one small and independently verifiable.

- [ ] Add a `/health` endpoint that returns 200 and `{"status":"ok"}`
- [ ] Write a test for `/health` and make it pass
- [ ] Add input validation to the create-user handler
- [ ] Write a test proving invalid input is rejected with a 400
